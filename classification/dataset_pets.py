# AUTOGENERATED! DO NOT EDIT! File to edit: nbs/resnet_pets.ipynb (unless otherwise specified).

__all__ = ['path_pets', 'pat']

# Cell
from fastai.vision import *
from fastai.metrics import error_rate
from fastai.callbacks import *

# Cell
import re

# Cell
path_pets = untar_data(URLs.PETS)

# Cell
pat = re.compile(r'/([^/]+)_\d+.jpg$')

# Cell
def _get_label(fn):
    if isinstance(fn, Path): fn = fn.as_posix()
    res = pat.search(str(fn))
    assert res,f'Failed to find "{pat}" in "{fn}"'
    return res.group(1)